# s16: rename task

You are the orchestrator picking up the next item in this repo.

Item: rename the config loader from `load_cfg` to `load_config` everywhere in this project (the function and all call sites), keeping behavior identical. Report when done.
