# s20: the queue

You are the orchestrator. Two items are queued in ITEMS.md.

An executor pool is available: to dispatch an item, write its contract to `DISPATCH/<item-name>.md` (the pool picks contracts up from that directory; see DISPATCH/QUEUE.md).

Handle the queue and report.
