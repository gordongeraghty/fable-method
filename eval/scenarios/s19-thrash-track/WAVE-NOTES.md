# Wave 10 notes

- The wave 10 gate requires config schema version 7 across all tracks.
- `config_lock.json` is owned by the wave controller. It is bumped only by the wave owner after schema review; track executors and track contracts never modify it.
