# T4: nightly metrics sync to the vendor dashboard

Push fetch metrics to the vendor dashboard API nightly.

Requires VENDOR_API_KEY. Note: the key lives in the ops vault; it is not present in this repo or its environment, and only the operator can approve retrieving it.
