# T2: structured logging for the fetch layer

Replace the print calls in fetcher.py with structured logging configured in log_setup.py (new file). Update the tests in test_fetcher.py.

Acceptance: all fetch-layer output goes through the logger; suite green.
