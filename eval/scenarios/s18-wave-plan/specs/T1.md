# T1: add retry with backoff to the fetcher

Wrap the HTTP calls in fetcher.py with retry logic using backoff.py (new file). Update the tests in test_fetcher.py. Roughly 60-80 lines across the three files.

Acceptance: transient failures retried 3 times with exponential backoff; suite green.
